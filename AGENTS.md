# MMViewer 開発ガイド

Windows 11向けの閲覧専用Markdown・Mermaidアプリ。C++20 / Win32 / GDI+を使い、配布物は `dist/MMViewer.exe`。Markdownはmd4c、Mermaidは同梱の公式エンジンとWebView2 Runtimeで描画する。

## 作業の進め方

- ユーザーの依頼と既存の承認範囲を優先する。実装依頼は、変更と必要な検証まで進める。起動も依頼されたら実画面で確認する。評価のみの依頼では編集・ビルド・起動へ広げない。
- 作業先の指定がなければ現在のブランチを使う。新しいブランチは、名前と理由を示して明示的な承認を得た場合だけ作成する。スクリプトやworktree経由でも同じ扱いとする。
- スキルの一般的な推奨から追加の承認待ちを作らない。スキルの指示が原因で停止が必要なら、該当ファイルと指示を示し、今回必要な判断を説明する。

## 守る設計

- フォルダ一覧からの除外・更新・タブ操作は、閲覧元の実ファイルを変更・削除しない。登録フォルダ、タブ、自動更新停止、読書位置の保持は別々に扱う。
- ファイルを引数で開く場合は既存ウィンドウへ渡す。起動・終了中や転送タイムアウトでも第2のUIを作らない。受信準備、終了状態、mutexの所有期間を含めて扱う。
- Mermaidはオフライン描画と `securityLevel: strict` を保つ。通常のC++ビルドにNode.jsや資産再生成を追加しない。
- 走査・通知・描画の変更では、UIスレッドに重い処理を戻さない。「非同期」と判断する際は、ワーカーの走査だけでなくUI反映・削除・終了時の処理も確認する。

## 必要な箇所から読む

| 対象 | 入口 |
| --- | --- |
| 利用者から見た現在の操作・制限 | [README.md](README.md) |
| タブ、ツリー、監視、セッション、ウィンドウ操作 | `src/native/main.cpp` |
| 本文のレイアウト、描画、検索、選択 | `src/native/viewer.cpp` |
| パス・文字コード・走査 / 起動時の確認 | `src/native/core.cpp` / `src/native/startup.cpp` |
| 単一起動とファイル転送 | `src/native/single_instance.h` |
| Mermaidの実描画 / 同梱資産の生成 | `src/native/mermaid_renderer.cpp` / `scripts/mermaid/` |
| ビルド、テスト選択、配布exeの更新、起動障害 | [mmviewer-validation](.agents/skills/mmviewer-validation/SKILL.md) |

`docs/` と `tasks/` は設計・調査・検証の履歴。関連する過去の判断が必要なときだけ読む。初期設計や過去のテスト件数を現在の実装・検証結果とみなさず、ソース・CMake・今回の実行結果と照合する。

## 検証と完了

変更に関係するテストを選ぶ。文言・指示ファイルだけの変更はリンク・構文・差分の確認を基本とし、アプリのビルドを必須にしない。回帰テストは利用者の操作や失敗条件を確かめるものにする。関連チェックが通った後の追加検証は、新たな変更・失敗・未解決の懸念がある場合に行う。

起動とMarkdown資料の表示は共通ユーザールールに従う。通常デスクトップの可視・画面内・応答する対象ウィンドウと、指定ファイルの表示まで確認する。完了報告では変更内容、実際に行った検証、残る未確認事項を簡潔に伝える。
