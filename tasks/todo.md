# 4回目: 残りの同期処理と走査・通知・描画の無駄を減らす 2026-09-06

別エージェントの評価（兄弟行の同期挿入、`SelectTreeFile` の全件走査、表・図の画面外描画、通知ごとの同期 syscall）と私の調査（走査の作り直し、直列ワーカー、`EnumerateFolder` の冗長 syscall、描画の小さな無駄）を統合。計画: `C:\Users\HT\.claude\plans\abstract-tinkering-coral.md`

## 実装

- [x] A 兄弟行の追加を区間ごとの Insert タスクへ（`Frame.stop`、先頭フレームは親を展開しない、200 行超は親を折りたたんで復元）
- [x] B `rowsByPath` 索引（`InsertRow` で登録、`TVN_DELETEITEMW` で値一致時のみ解除、`SelectTreeFile` はフォールバック付き）
- [x] C `PollWatches`: MODIFIED 非 md の早期スキップ、REMOVED/OLD_NAME は `knownFolders` だけ、ADDED/NEW_NAME の属性確認は再走査待ちの間だけ省略
- [x] D 走査: 再要求の合流（キャンセルしない）、`scanners[4]` プール、`Pending()`、`PostMessageW` 失敗時の復帰、`EnumerateFolder` の属性取得省略と連結
- [x] E 描画: 表の可視行、図の可視矩形、`HitLink` の二分探索、語の幅キャッシュ、ズームでキャッシュ維持、`HighlightCode`、ライブリサイズ保留、監視 0 件でタイマー停止

## テスト

- [x] host_tests: 既存フォルダ直下へ 3,000 ファイル追加の分割、移動で入ったフォルダ（REMOVED+ADDED）、大小文字改名・同パス再登録後の選択、走査中の再要求の合流、監視 0 件でタイマー停止、ゲートをプール全体へ
- [x] core_tests: `C:\` 連結、走査結果の同一性
- [x] viewer_tests / diagram_tests: 表の可視行、リンク当たり判定、幅キャッシュ・ズーム往復の画素一致、図の部分可視
- [x] tree_probe: 既存フォルダ直下への大量追加の計測

## 検証

- [x] `tree_probe C:\git\VagWork\Vag`: 走査 12.7〜13.7 秒 → 6.8 秒。`tree_probe C:\git\MMviewer`: 直下 3,000 件追加 6 スライス／0.2 秒、削除 10 スライス／0.34 秒（1行ずつの Delete タスクでは 1.4 秒 → 連続削除も1タスクにまとめて親を折りたたむ）
- [x] `.\scripts\build.ps1 -Test` 11 件成功（77.9 秒。sibling batch は最大スライス 37〜40 ms、5,000 行の表は 2.7 ms/フレーム）
- [x] ASan 版（`/EHsc` 付きで再構成）11 本すべて終了コード 0、報告なし
- [x] docs（性能最適化 4回目・検証結果）の更新。レビュー指摘 5 件（属性確認省略の条件、結果投函の再試行、キャッシュ鍵の粒度、辺ラベルの可視判定、索引の等価判定）を反映して再ビルド: 11 / 11 成功 77.8 秒、SHA-256 `3D6E83C9…DDF6`
- [ ] `probe-stall.ps1` の実アプリ計測（ユーザーの MMViewer を起動し直す必要があるため未実施）

## 更新時の一覧整理 2026-09-06

- [x] 「更新」/F5で、監視設定の完了を待たずに登録フォルダの再走査を開始。監視開始までの変更を拾うため、監視設定後の再走査も保持
- [x] 存在しない `.md` と空になった下位フォルダは差分で除去。ユーザー指定により、空の最上位フォルダ・登録・タブ本文・実ファイルは保持
- [x] 監視ワーカー停止中の更新、空の最上位フォルダ、監視開始前の削除を対象とする回帰テストを追加
- [ ] ビルド・回帰テスト実行・配布exeへの反映（未実施。直前の11件成功はこの変更前の結果）
